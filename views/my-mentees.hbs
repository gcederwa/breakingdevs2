<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <title>Document</title>
  <link rel="stylesheet" href="/styles.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-iYQeCzEYFbKjA/T2uDLTpkwGzCiq6soy8tYaI1GyVh/UjpbCx/TYkiZhlZB6+fzT" crossorigin="anonymous">
  <script src="https://code.jquery.com/jquery-1.10.2.js"></script>
  <script src="https://assets.calendly.com/assets/external/widget.js"></script>
  <title>Mentor Match - My Mentees</title>
</head>

<body>
  <header>

    <h1>Mentor Match</h1>
    <nav>
      {{#if userName}}
      <p>Hi {{userName}}!</p>
      {{/if}}
      <ul>
        <li><a href="/mentor-dashboard">Home</a></li>
        <li><a href="/my-mentees">My Mentees</a></li>
        <li><a href="/about">About Us</a></li>
        <!-- User Icon -->
        <li class="dropdown" id="userDropdown">
          <img src="{{#if profilePicture}}{{profilePicture}}{{else}}user-silhouette.png{{/if}}" id="userIcon"
            width="40px">
          <div class="dropdown-content" id="dropdownContent">
            <a href="/logout" id="logoutLink">Logout</a>
          </div>
        </li>
      </ul>
    </nav>
  </header>

  <div class="content-container">
    {{#if calendlyLink}}
    <div class="calendly-container">
      <h1>{{menteeName}}'s Availability:</h1>
      <div class="calendly-inline-widget" data-url="{{calendlyLink}}" style="min-width:320px;height:700px;"></div>
    </div>
    {{/if}}

    {{#if tasks}}
    <div class="tasks-container">
      <h2>{{menteeName}}'s Goals</h2>
      <ul id="tasks-list">
        {{#each tasks}}
        <li class="{{#if this.done}}checked{{/if}}">
          <div class="task-checkmark"></div>
          {{this.description}}
        </li>
        {{/each}}
      </ul>
    </div>
    {{/if}}
  </div>


  <table id="mentees-table">
    <thead>
      {{#unless showDetails}}
      <tr>
        <th>Name</th>
        <th>Role</th>
        <th>Details</th>
      </tr>
      {{/unless}}
    </thead>
    <tbody>
      {{#each mentees}}
      <tr>
        <td>{{name}}</td>
        <td>{{role}}</td>
        <td><a id="mentee-details-link" href="/my-mentees/{{id}}">View Details</a></td>
      </tr>
      {{/each}}
    </tbody>
  </table>



  <footer>
    <p>© 2023 Mentor Match. All rights reserved.</p>
  </footer>
</body>

</html>